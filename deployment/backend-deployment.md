# Backend Deployment Guide

## Status

`aurum-backend` is currently a placeholder repository (README only). There is no backend service to deploy yet.

When the backend is implemented, update this document with real build steps, environment variables, and hosting instructions.
